import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    img: {
        resizeMode: "contain",
        height: 150,
        width: "100%",
        marginTop: 20
    },
    content: {
        marginRight: 20,
        marginHorizontal: 20
    }
})